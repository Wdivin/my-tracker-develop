# My Tracker 1.0 — журнал сравнительной приёмки

## Назначение

Это нейтральный, версионируемый реестр результатов сравнительной приёмки независимых реализаций. Он не является частью исходников кандидатов и не заменяет их собственные отчёты.

Проверяемые snapshots:

| Вариант | Источник | Commit |
|---|---|---|
| So | приватный исходный репозиторий | `6ac693cf26980853db8dbafdb2532e93d92b87a9` |
| Cla | приватный исходный репозиторий | `04288936b5191a62f4194285501c3ae0a121ca2c` |

## Правила статусов

- **Подтверждён live** — воспроизведён на изолированном стенде.
- **Подтверждён по коду** — независимо установлен чтением конкретного кода.
- **Проверить live** — найден review; требуется воспроизведение на стенде.
- **Исправлен** — исправление принято только после повторного сценария.
- В реестр не записываются пароли, токены, cookie и иные секреты.

## Реестр

| ID | Вариант | Уровень | Статус | Суть и доказательство |
|---|---|---:|---|---|
| SO-001 | So | Critical | Подтверждён live | Чистая миграция MySQL 8.4 при включённом binary log и обычном DB-user останавливается на `CREATE TRIGGER` с Error 1419. Для успеха потребовался глобальный `log_bin_trust_function_creators=1`, чего инструкция установки не предусматривает. Стенд: 2026-07-17. |
| SO-002 | So | High | Подтверждён live | Login возвращает 502 `upstream sent too big header` при стандартных FastCGI buffers. Поставляемый `deploy/nginx/my-tracker.conf` также не задаёт увеличенные buffers. Стенд: 2026-07-17. |
| SO-003 | So | High | Подтверждён live | При недоступности Pwned Passwords установщик записал введённый пароль в stack trace и всё равно сообщил об успешной установке. Воспроизведено при `app:install`; секрет в этом документе намеренно не сохраняется. |
| SO-004 | So | High | Подтверждён по коду | API bulk резолвит `task_key` до ACL и раскрывает существование недоступных задач. |
| SO-005 | So | Medium | Подтверждён по коду | Перенос родительской задачи между проектами оставляет подзадачи в прежнем проекте. |
| SO-006 | So | Medium | Подтверждён по коду | Idempotency fingerprint не включает параметр ресурса из URL; одинаковый ключ может replay-ить ответ для другой задачи. |
| SO-007 | So | High | Проверить live | Backup/restore dump с DB DEFINER требует неописанных глобальных `SET_ANY_DEFINER`/`SYSTEM_USER`. |
| SO-008 | So | Mandatory | Подтверждён по коду | Validation и email не локализованы для ru/uk. |
| SO-009 | So | Medium | Подтверждён по коду | Смена конфигурации задачи не показывает пользователю фактически рассчитанное preview последствий до подтверждения. |
| SO-010 | So | Medium | Подтверждён по коду | Учёт времени не применяет ProjectStateGuard: возможна запись времени в suspended/completed проекте. |
| CL-001 | Cla | High | Подтверждён по коду | Обязательный комментарий workflow валидируется, но не сохраняется и не виден в ленте. |
| CL-002 | Cla | High | Подтверждён по коду | Bulk-операции из обязательного scope отсутствуют. |
| CL-003 | Cla | High | Подтверждён по коду | Перенос задачи в другой проект и смена типа недоступны из Web/API; реализованный код недостижим. |
| CL-004 | Cla | High | Подтверждён по коду | Optimistic locking только для tasks и opt-in; projects/workflows/types/config не покрыты. |
| CL-005 | Cla | High | Подтверждён по коду | API не эквивалентен Web в основном цикле: часть workflow/custom fields/assignment недоступна через API. |
| CL-006 | Cla | High | Подтверждён по коду | Корзина неполна, а force-delete может оставить осиротевшие attachment records. |
| CL-007 | Cla | Mandatory | Подтверждён по коду | Server-side validation не локализована для ru/uk. |
| CL-008 | Cla | Medium | Подтверждён по коду | Напоминания об overdue-задачах повторяются ежедневно, хотя brief запрещает повторы. |
| CL-009 | Cla | Medium | Подтверждён по коду | Уведомления не покрывают часть обязательных событий и не перепроверяют доступ получателя. |
| CL-010 | Cla | Medium | Подтверждён по коду | Поиск по custom fields даёт ложные совпадения и не использует FULLTEXT для основного поиска. |
| CL-011 | Cla | Medium | Подтверждён по коду | Health может вернуть 500 вместо controlled 503 при проблеме scheduler/БД. |

## Журнал live-приёмки

| Дата | Вариант | Среда | Результат |
|---|---|---|---|
| 2026-07-17 | So | Ubuntu 26.04, Docker, PHP 8.4.23, MySQL 8.4, Redis 7, Caddy external proxy | Инфраструктура и production frontend собраны; миграции воспроизвели SO-001. После временного server-level workaround все 16 миграций применились. Scheduler, Redis и health-ready работают. |
| 2026-07-17 | Cla | Та же изолированная Docker-среда | Общий внешний runtime-Dockerfile после добавления `ext-pcntl` и safe-directory успешно собрался. Это настройка тестового образа, не дефект кандидата. Чистая миграция MySQL 8.4 с включённым binary log прошла все 21 migration без глобального workaround. |
