# My Tracker 1.0 — журнал сравнительной приёмки

## Назначение

Это нейтральный, версионируемый реестр результатов сравнительной приёмки независимых реализаций. Он не является частью исходников кандидатов и не заменяет их собственные отчёты.

Проверяемые commits:

| Вариант | Репозиторий | Commit |
|---|---|---|
| Sol | `Wdivin/my-tracker-codex-sol` | `6ac693cf26980853db8dbafdb2532e93d92b87a9` |
| Claude | `Wdivin/my-tracker-claude` | `04288936b5191a62f4194285501c3ae0a121ca2c` |

## Правила статусов

- **Подтверждён live** — воспроизведён на изолированном стенде.
- **Подтверждён по коду** — независимо установлен чтением конкретного кода.
- **Проверить live** — найден review; требуется воспроизведение на стенде.
- **Исправлен** — исправление принято только после повторного сценария.
- В реестр не записываются пароли, токены, cookie и иные секреты.

## Реестр

| ID | Вариант | Уровень | Статус | Суть и доказательство |
|---|---|---:|---|---|
| SOL-001 | Sol | Critical | Подтверждён live | Чистая миграция MySQL 8.4 при включённом binary log и обычном DB-user останавливается на `CREATE TRIGGER` с Error 1419. Для успеха потребовался глобальный `log_bin_trust_function_creators=1`, чего инструкция установки не предусматривает. Стенд: 2026-07-17. |
| SOL-002 | Sol | High | Подтверждён live | Login возвращает 502 `upstream sent too big header` при стандартных FastCGI buffers. Поставляемый `deploy/nginx/my-tracker.conf` также не задаёт увеличенные buffers. Стенд: 2026-07-17. |
| SOL-003 | Sol | High | Подтверждён live | При недоступности Pwned Passwords установщик записал введённый пароль в stack trace и всё равно сообщил об успешной установке. Воспроизведено при `app:install`; секрет в этом документе намеренно не сохраняется. |
| SOL-004 | Sol | High | Подтверждён по коду | API bulk резолвит `task_key` до ACL и раскрывает существование недоступных задач. |
| SOL-005 | Sol | Medium | Подтверждён по коду | Перенос родительской задачи между проектами оставляет подзадачи в прежнем проекте. |
| SOL-006 | Sol | Medium | Подтверждён по коду | Idempotency fingerprint не включает параметр ресурса из URL; одинаковый ключ может replay-ить ответ для другой задачи. |
| SOL-007 | Sol | High | Проверить live | Backup/restore dump с DB DEFINER требует неописанных глобальных `SET_ANY_DEFINER`/`SYSTEM_USER`. |
| SOL-008 | Sol | Mandatory | Подтверждён по коду | Validation и email не локализованы для ru/uk. |
| SOL-009 | Sol | Medium | Подтверждён по коду | Смена конфигурации задачи не показывает пользователю фактически рассчитанное preview последствий до подтверждения. |
| SOL-010 | Sol | Medium | Подтверждён по коду | Учёт времени не применяет ProjectStateGuard: возможна запись времени в suspended/completed проекте. |
| CLA-001 | Claude | High | Подтверждён по коду | Обязательный комментарий workflow валидируется, но не сохраняется и не виден в ленте. |
| CLA-002 | Claude | High | Подтверждён по коду | Bulk-операции из обязательного scope отсутствуют. |
| CLA-003 | Claude | High | Подтверждён по коду | Перенос задачи в другой проект и смена типа недоступны из Web/API; реализованный код недостижим. |
| CLA-004 | Claude | High | Подтверждён по коду | Optimistic locking только для tasks и opt-in; projects/workflows/types/config не покрыты. |
| CLA-005 | Claude | High | Подтверждён по коду | API не эквивалентен Web в основном цикле: часть workflow/custom fields/assignment недоступна через API. |
| CLA-006 | Claude | High | Подтверждён по коду | Корзина неполна, а force-delete может оставить осиротевшие attachment records. |
| CLA-007 | Claude | Mandatory | Подтверждён по коду | Server-side validation не локализована для ru/uk. |
| CLA-008 | Claude | Medium | Подтверждён по коду | Напоминания об overdue-задачах повторяются ежедневно, хотя brief запрещает повторы. |
| CLA-009 | Claude | Medium | Подтверждён по коду | Уведомления не покрывают часть обязательных событий и не перепроверяют доступ получателя. |
| CLA-010 | Claude | Medium | Подтверждён по коду | Поиск по custom fields даёт ложные совпадения и не использует FULLTEXT для основного поиска. |
| CLA-011 | Claude | Medium | Подтверждён по коду | Health может вернуть 500 вместо controlled 503 при проблеме scheduler/БД. |

## Журнал live-приёмки

| Дата | Вариант | Среда | Результат |
|---|---|---|---|
| 2026-07-17 | Sol | Ubuntu 26.04, Docker, PHP 8.4.23, MySQL 8.4, Redis 7, Caddy external proxy | Инфраструктура и production frontend собраны; миграции воспроизвели SOL-001. После временного server-level workaround все 16 миграций применились. Scheduler, Redis и health-ready работают. |
